[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "udvt-suite"
version = "3.0.0"
description = "Unified Dynamic Vacuum Theory Computational Suite"
authors = [
    {name = "Myo Sett Naing"}
]
dependencies = [
    "numpy>=1.20.0",
    "pandas>=1.3.0",
    "scipy>=1.7.0",
    "matplotlib>=3.4.0"
]
