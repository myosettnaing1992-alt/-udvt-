[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "udvt"
version = "3.0.0"
authors = [{ name="Myo Sett Naing", email="your-email@example.com"}]

Description = "Simulation suite for the Unified Dynamic Vacuum Theory"
readme = "README.md"
requires-python = "> = 3.9"
dependencies = [ "numpy", "scipy","matplotlib"]
